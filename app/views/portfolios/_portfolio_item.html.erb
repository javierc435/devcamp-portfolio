<div class="card" data-id="<%= x.id %>" >

  <%= image_tag portfolio_img x.thumb_image, 'thumb' %>
  <p class="card-text">
          <span><%= link_to x.title, portfolio_show_path(x) %></span> <%= x.subtitle %>
  </p>

</div>